import { ProjectEstimator } from "@/components/contact/ProjectEstimator";

export const metadata = {
  title: "Contact & Project Inquiry",
  description: "Plan your website, application, or software project, receive an indicative range, and send a complete inquiry to RIITHIS.",
};

export default function ContactPage() {
  return <ProjectEstimator />;
}
