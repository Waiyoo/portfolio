import { ProjectEstimator } from "@/components/contact/ProjectEstimator";

export const metadata = {
  title: "Start a Project | RIITHIS",
  description: "Build a clear brief for your website, application, business system, or integration and receive an indicative project range.",
};

export default function StartProjectPage() {
  return <ProjectEstimator />;
}
